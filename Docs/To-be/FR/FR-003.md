# F-003 — Создание пациента
![TO-BE](https://img.shields.io/badge/TO--BE-In%20Progress-yellow)
![MVP](https://img.shields.io/badge/Релиз-MVP-blue)

<table>
  <tr>
    <td>Актор</td>
    <td>Сотрудник (STAFF)</td>
  </tr>
  <tr>
    <td>Сервисы</td>
    <td>patient-service, billing-service, auth-service, notification-service, kafka</td>
  </tr>
</table>

## Назначение

Сотрудник клиники регистрирует нового пациента в системе через модальную форму. После создания пациент получает на email приветственное письмо с логином, сгенерированным паролем и ссылкой на личный кабинет.

---

## AS-IS процесс

```
[вставить BPMN]
```

**Описание текущего поведения:**

Сотрудник заполняет форму из 5 полей: `name` (одно поле ФИО строкой), `email`, `address` (одна строка), `dateOfBirth`, `registeredDate` (вводится вручную). После сохранения:

1. `patient-service`сохраняет пациента в `patient-service-db`, таблица `patient`(адрес — одна строка, ФИО - одна строка)
2. `billing-service` создаёт биллинговый аккаунт (gRPC, заглушка — ничего не сохраняет)
3. `Kafka` публикует событие `PATIENT_CREATED` в топик `patient` (без настроенного DLQ)
4. `analytics-service` потребляет событие (логирует, без side-effect)
5. Уведомление пациенту **не отправляется**
6. Аккаунт для входа в ЛК **не создаётся**
7. `patient-service` возвращает `frontend` статус-код `200 OK` + `PatientResponseDTO`

**Известные проблемы AS-IS:**

- `name` — одна строка, невозможен корректный поиск по фамилии/имени отдельно (для будущих фич)
- `address` — одна строка, нет нормализации, невозможна аналитика по городам/районам (для будущих фич)
- `registeredDate` передаётся клиентом (некорректно архитектурно)
- `POST /patients` возвращает `200 OK` вместо `201 Created` (некорректно архитектурно)
- `billing-service` не сохраняет данные — (заглушка)
- `notification-service` отсутствует — пациент не получает доступ к ЛК
- `Kafka` (silent data loss): ошибка публикации проглатывается (`log.error` без retry)
- Нет транзакционности — при падении gRPC или Kafka пациент сохранён в БД, но биллинг и уведомление не созданы

---

## TO-BE процесс

```
[вставить BPMN]
```

**Описание целевого поведения:**

Сотрудник заполняет форму из **9 полей**: фамилия, имя, отчество (необязательное), email, город, улица, дом, квартира (необязательная), дата рождения. Нажимает кнопку "Сохраненить":

**Синхронная часть (в рамках HTTP запроса):**

1. `patient-service` открывает транзакцию.
2. `patient-service` автоматически проставляет `registeredDate = LocalDate.now()`.
3. `patient-service`сохраняет в `patient-service-db` адрес в таблицу `addresses`, пациента в таблицу `patients` (связь `@OneToOne`).
4. Записывает событие `PATIENT_CREATED` в `patient-service-db` в таблицу `outbox_events` со статусом `PENDING`.
5. Транзакция коммитится — шаги 2, 3, 4 атомарны: либо все вместе, либо откат.
6. `patient-service` возвращает `frontend` статус-код `201 CREATED` + `PatientResponseDTO`.
7. `frontend` закрывает модалку, показывает тост «Пациент успешно добавлен».

    
    После шага 6 HTTP соединение закрыто. Клиент не ждёт выполнения асинхронных шагов ниже.


**Асинхронная часть (вне HTTP запроса):**


8. `OutboxPoller` (@Scheduled каждые 5 сек) читает события со статусом `PENDING`.
9. `patient-service` отправляет событие `PATIENT_CREATED` в `Kafka` топик `patient`. 
10. `patient-service` помечает событие как `PROCESSED` в `outbox_events`.
11. `billing-service` (Kafka consumer) потребляет событие, создаёт биллинговый аккаунт в `billing-service-db`. 
12. `notification-service` (Kafka consumer) потребляет событие, генерирует временный пароль.
13. `notification-service` создаёт аккаунт пациента через `POST /auth/register` в `auth-service`.
14. `notification-service` отправляет пациенту welcome email с логином, паролем и ссылкой на ЛК.
15. `analytics-service` (Kafka consumer) потребляет событие (логирует, без side-effect)

---

## Frontend

<details>
<summary><b><font color="#2196F3">Макеты (Figma)</font></b></summary>

https://www.figma.com/design/nKnObYbrARzxXtGOSf1osv/Healthcare-System?node-id=28-268&m=dev

| Фрейм | Описание                                               |
|-------|--------------------------------------------------------|
| Add Patient — Default | Пустая форма |
| Add Patient — Filled | Все обязательные поля заполнены |
| Add Patient — Loading | Нажата кнопка «Сохранить»                              |
| Add Patient — Error Validation | Ошибка клиентской валидации                            |
| Add Patient — Error 409 | Пользователь с таким email уже существует              |
| Add Patient — Error 500 | Ошибка сервера. Попробуйте позже                       |
| Add Patient — Success | Пациент успешно добавлен |
 </details> 

---
<details>
<summary><b><font color="#2196F3">Общие критерии для всех фреймов</font></b></summary>

**Структура формы:**
- Форма разделена на три секции: «Персональные данные», «Контактные данные», «Адрес проживания»
- Поля в секции «Персональные данные»: Фамилия, Имя, Отчество, Дата рождения
- Поля в секции «Контактные данные»: Email
- Поля в секции «Адрес проживания»: Город, Улица, Дом, Квартира
- Поле `registeredDate` отсутствует в форме — проставляется бэкендом автоматически

**Необязательные поля:**
- Отчество
- Квартира 

**Placeholders:**
- Фамилия: «Введите фамилию»
- Имя: «Введите имя»
- Отчество: «Введите отчество»
- Дата рождения: «Введите дату рождения в формате ГГГГ-ММ-ДД»
- Email: «Введите email»
- Город: «Введите город»
- Улица: «Введите улицу»
- Дом: «Введите дом»
- Квартира: «Введите квартиру»

**Поведение модального окна:**
- Открывается по нажатию кнопки «Добавить пациента» на странице списка пациентов
- Фон страницы размывается (блюр) при открытии модального окна
- Кнопка «Отменить» — закрывает модальное окно и очищает все поля без сохранения
- Клик вне модалки — закрывает модальное окно и очищает все поля без сохранения
- Клавиша `Escape` — закрывает модальное окно без сохранения
</details> 

---

<details>
<summary><b><font color="#2196F3">Валидация полей</font></b></summary>

<table>
  <thead>
    <tr>
      <th>Поле</th>
      <th>Обязательное</th>
      <th>Правило</th>
      <th>Когда проверяется</th>
      <th>Сообщение об ошибке</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Фамилия</td>
      <td>Да</td>
      <td>max 100 символов</td>
      <td>При потере фокуса</td>
      <td>«Не более 100 символов»</td>
    </tr>
    <tr>
      <td>Имя</td>
      <td>Да</td>
      <td>max 100 символов</td>
      <td>При потере фокуса</td>
      <td>«Не более 100 символов»</td>
    </tr>
    <tr>
      <td>Отчество</td>
      <td>Нет</td>
      <td>max 100 символов</td>
      <td>При потере фокуса</td>
      <td>«Не более 100 символов»</td>
    </tr>
    <tr>
      <td rowspan="2">Email</td>
      <td rowspan="2">Да</td>
      <td>формат RFC 5322</td>
      <td>При потере фокуса</td>
      <td>«Введите корректный email»</td>
    </tr>
    <tr>
      <td>уникальность</td>
      <td>При ответе <code>400</code> от сервера</td>
      <td>«Пользователь с таким email уже существует»</td>
    </tr>
    <tr>
      <td>Город</td>
      <td>Да</td>
      <td>max 100 символов</td>
      <td>При потере фокуса</td>
      <td>«Не более 100 символов»</td>
    </tr>
    <tr>
      <td>Улица</td>
      <td>Да</td>
      <td>max 200 символов</td>
      <td>При потере фокуса</td>
      <td>«Не более 200 символов»</td>
    </tr>
    <tr>
      <td>Дом</td>
      <td>Да</td>
      <td>max 20 символов</td>
      <td>При потере фокуса</td>
      <td>«Не более 20 символов»</td>
    </tr>
    <tr>
      <td>Квартира</td>
      <td>Нет</td>
      <td>max 20 символов</td>
      <td>При потере фокуса</td>
      <td>«Не более 20 символов»</td>
    </tr>
    <tr>
      <td rowspan="3">Дата рождения</td>
      <td rowspan="3">Да</td>
      <td>формат <code>ГГГГ-ММ-ДД</code></td>
      <td>При потере фокуса</td>
      <td>«Используйте формат ГГГГ-ММ-ДД»</td>
    </tr>
    <tr>
      <td>не в будущем</td>
      <td>При потере фокуса</td>
      <td>«Дата не может быть в будущем»</td>
    </tr>
    <tr>
      <td>не ранее 1900-01-01</td>
      <td>При потере фокуса</td>
      <td>«Введите корректную дату»</td>
    </tr>
  </tbody>
</table>
</details>

---
<details>
<summary><b><font color="#2196F3">Add Patient — Default</font></b></summary>

### 

```
[вставить скриншот фрейма]
```

**Критерии приёмки:**
- Все поля пустые, показаны placeholders
- Кнопка «Сохранить» задизейблена 
- Ни одно поле не подсвечено красным
- Секции отображаются с заголовками: «Персональные данные», «Контактные данные», «Адрес проживания» с соответствующими лейблами
</details> 

---
<details>
<summary><b><font color="#2196F3">Add Patient — Filled</font></b></summary>

```
[вставить скриншот фрейма]
```

**Критерии приёмки:**
- Все обязательные поля заполнены (Фамилия, Имя, Email, Город, Улица, Дом, Дата рождения)
- Необязательные поля (Отчество, Квартира) могут быть пустыми — это не блокирует кнопку
- Кнопка «Сохранить» активна
- Кнопка активируется в момент заполнения последнего обязательного поля
- Кнопка деактивируется если очистить любое обязательное поле
- Введённые данные корректно отображаются в полях без обрезки
</details> 

---
<details>
<summary><b><font color="#2196F3">Add Patient — Loading</font></b></summary>


```
[вставить скриншот фрейма]
```

**Критерии приёмки:**
- Все поля заблокированы для редактирования (визуально не меняются)
- Кнопка «Сохранить» задизейблена, показывает спиннер + текст «Сохранить»
- Кнопка «Отменить» заблокирована — нельзя закрыть модальное окно во время запроса
- Клик вне модального окна не закрывает её во время запроса
- Данные в полях сохранены (форма не сбрасывается)
</details> 

---
<details>
<summary><b><font color="#2196F3">Add Patient — Error validation</font></b></summary>


```
[вставить скриншот фрейма]
```

**Критерии приёмки:**
- Поле с ошибкой подсвечено красной границей
- Под полем с ошибкой отображается красный текст с описанием нарушенного правила
- Кнопка «Сохранить» задизейблена — запрос на сервер не отправляется
- Остальные поля без подсветки ошибок
- Ошибка проверяется при потере фокуса с поля, не при каждом нажатии клавиши
- При исправлении значения и потере фокуса с поля — если значение корректно, ошибка скрывается
- Если ошибок несколько — подсвечиваются все поля с ошибками одновременно
</details> 

---
<details>
<summary><b><font color="#2196F3">Add Patient — Error 409 (email уже существует)</font></b></summary>

```
[вставить скриншот фрейма]
```

**Критерии приёмки:**
- Поле Email подсвечено красной границей
- Под полем Email красный текст: «Пользователь с таким email уже существует»
- Остальные поля без подсветки ошибок, данные сохранены
- Кнопка «Сохранить» активна — пользователь может исправить email и повторить запрос
- При исправлении значения в поле email и потере фокуса с поля — красная подсветка и сообщение скрываются
</details> 

---
<details>
<summary><b><font color="#2196F3">Add Patient — Error 500 (сервер недоступен)</font></b></summary>

```
[вставить скриншот фрейма]
```

**Критерии приёмки:**
- Модальное окно остаётся открытом, все поля сохранены
- Над кнопкой «Сохранить» отображается красный текст: «Ошибка сервера. Попробуйте позже»
- Ни одно поле не подсвечено красным (ошибка не связана с конкретным полем)
- Кнопка «Сохранить» активна — пользователь может повторить попытку
- Кнопка «Отменить» доступна — пользователь может закрыть форму
- Сообщение об ошибке скрывается при повторной отправке формы - нажатии кнопки "Сохранить"
</details> 

---
<details>
<summary><b><font color="#2196F3">Add Patient — Success</font></b></summary>

```
[вставить скриншот фрейма]
```

**Критерии приёмки:**
- Модальное окно закрылось сразу после получения `201 Created` от сервера
- Список пациентов обновился без перезагрузки страницы — новый пациент виден в списке
- Тост-уведомление в правом верхнем углу: «✓ Пациент успешно добавлен» 
- Тост автоматически исчезает через 3 секунды
- Форма очищена (при повторном открытии модалки поля пустые)
</details> 


---

## Backend

<details>
<summary><b><font color="#2196F3">patient-service</font></b></summary>

### C4 Component — изменения

```
[вставить C4 diff]
```

**Изменения:**
- 🔴 `Patient.name: String` — удалено, заменено на три поля
- 🟢 `Patient.lastName`, `Patient.firstName`, `Patient.middleName` — новые поля
- 🔴 `Patient.address: String` — удалено, заменено связью с `Address`
- 🟢 Новая сущность `Address` и таблица `addresses`
- 🟢 Связь `Patient @OneToOne Address` (cascade = ALL)
- 🔴 `PatientRequestDTO.name: String` — удалено
- 🟢 `PatientRequestDTO`: добавлены `lastName`, `firstName`, `middleName`, вложенный `AddressDTO`
- 🔴 `PatientResponseDTO.name: String` — удалено
- 🟢 `PatientResponseDTO`: добавлены `lastName`, `firstName`, `middleName`, вложенный `AddressDTO`
- 🔴 `PatientRequestDTO.registeredDate` — удалено
- 🟢 `PatientMapper.toModel()` — `registeredDate = LocalDate.now()`
- 🟢 `POST /patients` — код ответа `200 OK` → `201 Created`
- 🟢 `PatientEvent` protobuf — `name` → `lastName`, `firstName`, `middleName`

---

## ERD

```
[вставить ERD]
```

**Таблицы patient-service (TO-BE):**

```
patients
─────────────────────────────────────
id               UUID        PK
last_name        VARCHAR     NOT NULL
first_name       VARCHAR     NOT NULL
middle_name      VARCHAR     NULL
email            VARCHAR     UNIQUE NOT NULL
date_of_birth    DATE        NOT NULL
registered_date  DATE        NOT NULL
address_id       UUID        FK → addresses.id UNIQUE

addresses
─────────────────────────────────────
id               UUID        PK
city             VARCHAR     NOT NULL
street           VARCHAR     NOT NULL
house            VARCHAR     NOT NULL
apartment        VARCHAR     NULL
```

---

### POST /patients — Создание пациента

> ⚠️ Эндпоинт существует в AS-IS. Изменения выделены.

**Метод:** `POST`
**Путь:** `/patients`
**Аутентификация:** `Authorization: Bearer <token>`

<details>
<summary>Входные параметры (Request Body)</summary>

```json
{
  "lastName": "Иванов",
  "firstName": "Иван",
  "middleName": "Иванович",
  "email": "ivanov@mail.ru",
  "address": {
    "city": "Москва",
    "street": "Проспект Мира",
    "house": "10",
    "apartment": "110"
  },
  "dateOfBirth": "1980-01-01"
}
```

| Поле | Тип | Обязательное | Валидация | AS-IS |
|------|-----|-------------|-----------|-------|
| ~~`name`~~ | ~~`String`~~ | ~~✅~~ | ~~`@NotBlank`, max 100~~ | 🔴 удалено |
| 🟢 `lastName` | `String` | ✅ | `@NotBlank`, max 100 | новое |
| 🟢 `firstName` | `String` | ✅ | `@NotBlank`, max 100 | новое |
| 🟢 `middleName` | `String` | ❌ | max 100, nullable | новое |
| `email` | `String` | ✅ | `@NotBlank`, `@Email` | без изменений |
| ~~`address`~~ | ~~`String`~~ | ~~✅~~ | ~~`@NotBlank`~~ | 🔴 заменено |
| 🟢 `address.city` | `String` | ✅ | `@NotBlank`, max 100 | новое |
| 🟢 `address.street` | `String` | ✅ | `@NotBlank`, max 200 | новое |
| 🟢 `address.house` | `String` | ✅ | `@NotBlank`, max 20 | новое |
| 🟢 `address.apartment` | `String` | ❌ | max 20, nullable | новое |
| `dateOfBirth` | `String` | ✅ | `@NotBlank`, формат `YYYY-MM-DD` | без изменений |
| ~~`registeredDate`~~ | ~~`String`~~ | ~~✅~~ | ~~`@NotBlank`~~ | 🔴 удалено |

</details>

<details>
<summary>Выходные параметры (Response)</summary>

```json
{
  "id": "123e4567-e89b-12d3-a456-426614174000",
  "lastName": "Иванов",
  "firstName": "Иван",
  "middleName": "Иванович",
  "email": "ivanov@mail.ru",
  "address": {
    "id": "456e7890-e89b-12d3-a456-426614174000",
    "city": "Москва",
    "street": "Проспект Мира",
    "house": "10",
    "apartment": "110"
  },
  "dateOfBirth": "1980-01-01",
  "registeredDate": "2026-03-15"
}
```

| Поле | Тип | Описание | AS-IS |
|------|-----|---------|-------|
| `id` | `String (UUID)` | ID пациента | без изменений |
| ~~`name`~~ | ~~`String`~~ | ~~ФИО строкой~~ | 🔴 удалено |
| 🟢 `lastName` | `String` | Фамилия | новое |
| 🟢 `firstName` | `String` | Имя | новое |
| 🟢 `middleName` | `String\|null` | Отчество | новое |
| `email` | `String` | Email | без изменений |
| ~~`address`~~ | ~~`String`~~ | ~~Адрес строкой~~ | 🔴 заменено |
| 🟢 `address` | `AddressDTO` | Вложенный объект адреса | новое |
| `dateOfBirth` | `String` | Дата рождения `YYYY-MM-DD` | без изменений |
| 🟢 `registeredDate` | `String` | Дата регистрации `YYYY-MM-DD` | новое в response |

</details>

<details>
<summary>Коды ответов</summary>

| Код | Описание | Тело ответа |
|-----|---------|-------------|
| ~~`200 OK`~~ | ~~Успешное создание~~ | 🔴 удалено |
| 🟢 `201 Created` | Успешное создание | `PatientResponseDTO` |
| `400 Bad Request` | Email уже существует | `{"message": "Email address already exists"}` |
| `400 Bad Request` | Ошибка валидации | `{"field": "message", ...}` |
| `401 Unauthorized` | Токен невалиден | — |

</details>

<details>
<summary>Алгоритм</summary>

```
1. Валидация входных параметров (@Validated)
   → ошибка → 400 + map полей с сообщениями

2. Проверка уникальности email
   patientRepository.existsByEmail(email)
   → существует → EmailAlreadyExistsException → 400

3. Маппинг DTO → модель (PatientMapper.toModel)
   patient.lastName   = dto.lastName
   patient.firstName  = dto.firstName
   patient.middleName = dto.middleName  // nullable
   patient.email      = dto.email
   patient.dateOfBirth = LocalDate.parse(dto.dateOfBirth)
   🟢 patient.registeredDate = LocalDate.now()
   🟢 address = new Address(city, street, house, apartment)
   🟢 patient.address = address

4. Сохранение в БД (каскадное)
   patientRepository.save(patient)
   // Address сохраняется автоматически через cascade = ALL

5. Создание биллингового аккаунта (gRPC)
   billingServiceGrpcClient.createBillingAccount(
     id,
     lastName + " " + firstName + " " + middleName,
     email
   )

6. Публикация события в Kafka
   topic: "patient"
   event: PatientEvent {
     patientId,
     🟢 lastName, firstName, middleName,
     email,
     eventType: "PATIENT_CREATED"
   }

7. Возврат PatientResponseDTO
   🟢 HTTP 201 Created
```

</details>

<details>
<summary>Sequence diagram</summary>

```
[вставить sequence diagram]
```

Участники: Frontend → API Gateway → patient-service → patient_db
→ billing-service (gRPC)
→ Kafka Broker → notification-service

</details>

</details>

---

<details>
<summary><b><font color="#2196F3">billing-service</font></b></summary>

### C4 Component — изменения

```
[вставить C4 diff]
```

**Изменения:**
- 🟢 Добавлена `billing_db` (PostgreSQL)
- 🟢 Добавлена таблица `billing_accounts`
- 🟢 gRPC метод `createBillingAccount` реализован — сохраняет запись в БД вместо заглушки
- 🟢 Добавлен `BillingAccountRepository` (Spring Data JPA)
- 🟢 Добавлена сущность `BillingAccount`

---

### Таблица billing_accounts

| Колонка | Тип | Описание |
|---------|-----|---------|
| `id` | `UUID` | PK, генерируется автоматически |
| `patient_id` | `UUID` | ID пациента из patient-service, UNIQUE |
| `account_number` | `String` | Формат `BILL-{UUID}`, UNIQUE |
| `status` | `String` | `ACTIVE` / `SUSPENDED` |
| `created_at` | `LocalDateTime` | Проставляется автоматически |

---

### gRPC метод createBillingAccount

> ⚠️ Метод существует в AS-IS как заглушка. В TO-BE реализуется полностью.

**Входные параметры (без изменений):**

| Поле | Тип | Описание |
|------|-----|---------|
| `patientId` | `string` | UUID пациента |
| `name` | `string` | ФИО пациента (для отображения) |
| `email` | `string` | Email пациента |

**Выходные параметры:**

| Поле | Тип | AS-IS | TO-BE |
|------|-----|-------|-------|
| ~~пустой ответ~~ | — | 🔴 заглушка | — |
| 🟢 `accountNumber` | `string` | — | `BILL-{UUID}` |
| 🟢 `patientId` | `string` | — | UUID пациента |
| 🟢 `status` | `string` | — | `ACTIVE` |

<details>
<summary>Алгоритм</summary>

```
1. Получение gRPC запроса { patientId, name, email }

2. Проверка уникальности patientId
   billingAccountRepository.existsByPatientId(patientId)
   → существует → gRPC статус ALREADY_EXISTS

3. Генерация номера аккаунта
   accountNumber = "BILL-" + UUID.randomUUID()

4. Создание и сохранение BillingAccount
   BillingAccount {
     id:            UUID.randomUUID(),
     patientId:     patientId,
     accountNumber: accountNumber,
     status:        "ACTIVE",
     createdAt:     LocalDateTime.now()
   }
   billingAccountRepository.save(account)

5. Возврат gRPC ответа { accountNumber, patientId, status }
```

</details>

<details>
<summary>Коды ошибок и обработка</summary>

| Ситуация | AS-IS | TO-BE |
|----------|-------|-------|
| Ошибка сохранения в БД | — (заглушка) | gRPC статус `INTERNAL` → patient-service логирует |
| Дублирующийся `patientId` | — | gRPC статус `ALREADY_EXISTS` |

</details>

</details>

---

<details>
<summary><b><font color="#2196F3">auth-service</font></b></summary>

### C4 Component — изменения

```
[вставить C4 diff]
```

**Изменения:**
- 🟢 Новый внутренний эндпоинт `POST /auth/register`

---

### POST /auth/register — Создание аккаунта пациента

> 🟢 Новый эндпоинт — реализуется с нуля.

**Метод:** `POST`
**Путь:** `/auth/register`
**Аутентификация:** Internal only (недоступен через API Gateway)

<details>
<summary>Входные параметры (Request Body)</summary>

```json
{
  "email": "ivanov@mail.ru",
  "password": "Xk9mP2qL4rN8",
  "role": "PATIENT"
}
```

| Поле | Тип | Обязательное | Описание |
|------|-----|-------------|---------|
| `email` | `String` | ✅ | Используется как логин |
| `password` | `String` | ✅ | Сгенерированный временный пароль |
| `role` | `String` | ✅ | `PATIENT` |

</details>

<details>
<summary>Выходные параметры (Response)</summary>

```json
{
  "userId": "789e0123-e89b-12d3-a456-426614174000",
  "email": "ivanov@mail.ru",
  "role": "PATIENT"
}
```

</details>

<details>
<summary>Коды ответов</summary>

| Код | Описание |
|-----|---------|
| `201 Created` | Аккаунт успешно создан |
| `400 Bad Request` | Email уже зарегистрирован |
| `403 Forbidden` | Вызов не из внутренней сети |

</details>

<details>
<summary>Алгоритм</summary>

```
1. Проверка источника запроса (internal only)
2. Проверка уникальности email
3. Хэширование пароля (BCrypt)
4. Сохранение в auth_db с ролью PATIENT
5. Возврат userId + email + role
```

</details>

</details>

---

<details>
<summary><b><font color="#2196F3">notification-service</font></b></summary>


### C4 Component — изменения

```
[вставить C4 diff]
```

**Изменения:**
- 🟢 Новый сервис — реализуется с нуля
- 🟢 Потребляет `PATIENT_CREATED` из топика `patient`
- 🟢 Интегрируется с auth-service для создания аккаунта
- 🟢 Отправляет welcome email через SMTP

---

### Логика обработки события PATIENT_CREATED

**Триггер:** Kafka событие `PatientEvent { eventType: "PATIENT_CREATED" }`
**Топик:** `patient`

<details>
<summary>Алгоритм</summary>

```
1. Получение события из Kafka
   PatientEvent { patientId, lastName, firstName, middleName, email, eventType }

2. Idempotency check
   Проверка по patientId — не обрабатывать повторно

3. Генерация временного пароля
   Случайная строка: 12 символов (буквы + цифры)

4. Создание аккаунта в auth-service
   POST /auth/register { email, password, role: "PATIENT" }

5. Отправка welcome email
   Получатель: email
   Тема: «Добро пожаловать в Healthcare-System»
   Содержание:
     - «Здравствуйте, {firstName} {middleName}»
     - Логин: email
     - Временный пароль
     - Ссылка: https://healthcare-system.ru/login
     - Рекомендация сменить пароль при первом входе
```

</details>

<details>
<summary>Коды ошибок и обработка</summary>

| Ситуация | AS-IS | TO-BE |
|----------|-------|-------|
| Ошибка создания аккаунта в auth-service | — | Retry 3 раза → DLQ `patient.DLT` |
| Ошибка отправки email | — | Retry 3 раза → DLQ `patient.DLT` |
| Дублирующееся событие | — | Idempotency check по `patientId` |

</details>

</details>

---

<details>
<summary><b><font color="#2196F3">Kafka</font></b></summary>

## Kafka события

| Топик | Producer | Consumer | Event type | Изменения |
|-------|---------|---------|------------|-----------|
| `patient` | patient-service | analytics-service | `PATIENT_CREATED` | `name` → 🟢 `lastName`, `firstName`, `middleName` |
| `patient` | patient-service | 🟢 notification-service | `PATIENT_CREATED` | 🟢 новый consumer |
| 🟢 `patient.DLT` | notification-service | — | — | 🟢 новый топик (DLQ) |

</details>
---

## Definition of Done

**patient-service:**
- [ ] `Patient` модель: `name` → `lastName`, `firstName`, `middleName`
- [ ] Сущность `Address` и таблица `addresses` созданы
- [ ] Связь `Patient @OneToOne Address` (cascade = ALL) настроена
- [ ] `PatientRequestDTO`: обновлён (новые поля ФИО + вложенный `AddressDTO`)
- [ ] `PatientResponseDTO`: обновлён аналогично
- [ ] `PatientMapper` обновлён
- [ ] `registeredDate` удалено из `PatientRequestDTO`, устанавливается как `LocalDate.now()`
- [ ] `POST /patients` возвращает `201 Created`
- [ ] `PatientEvent` protobuf обновлён: `name` → `lastName`, `firstName`, `middleName`

**billing-service:**
- [ ] `billing_db` и таблица `billing_accounts` добавлены
- [ ] gRPC метод `createBillingAccount` сохраняет запись в БД
- [ ] Возвращает `accountNumber` формата `BILL-{UUID}`

**auth-service:**
- [ ] `POST /auth/register` реализован (internal only)

**notification-service:**
- [ ] Сервис реализован с нуля
- [ ] Потребляет `PATIENT_CREATED` из топика `patient`
- [ ] Генерирует временный пароль, создаёт аккаунт через auth-service
- [ ] Welcome email отправляется пациенту
- [ ] DLQ топик `patient.DLT` настроен
- [ ] Idempotency check реализован

**Общее:**
- [ ] Макеты Figma обновлены (поля ФИО и адреса)
- [ ] ERD обновлён
- [ ] Все изменения покрыты тестами